services:
  backend:
    build: .
    container_name: django-docker
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./core:/app
#    depends_on:
#      - db